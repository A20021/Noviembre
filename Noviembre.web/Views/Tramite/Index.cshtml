@using Noviembre.Core.Entidades
@model List<Tramite>

<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Tramites</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-body">

                        <a href="@Url.Action("Registro", "Tramite")" class="btn btn-primary">Agregar nuevo</a>

                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Nombre</th>
                                        <th>Id</th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (Tramite t in Model)
                                    {
                                        <tr>
                                            <td>
                                                @t.Nombre
                                            </td>
                                            <td>
                                                @t.Id
                                            </td>
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

