@using Noviembre.Core.Entidades
@model List<DocumentoNacionalidad>

<div id="page-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Documentos de nacionalidad</h1>
            </div>
        </div>

        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-body">

                        <a href="@Url.Action("Registro", "DocumentoNacionalidad")" class="btn btn-primary">Agregar nuevo</a>

                        <div class="table-responsive">
                            <table class="table table-striped">
                                <thead>
                                    <tr>
                                        <th>Id</th>
                                        <th>Nombre</th>
                                    </tr>
                                </thead>

                                <tbody>
                                    @foreach (DocumentoNacionalidad d in Model)
                                    {
                                        <tr>
                                            <td>
                                                @d.Id
                                            </td>
                                            <td>
                                                @d.Nombre
                                            </td>
                                        </tr>
                                    }

                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

